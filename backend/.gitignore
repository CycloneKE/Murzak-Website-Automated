# Secrets - never commit
.env
.env.*
!.env.example

# Dependencies
node_modules

# Logs
logs
*.log
npm-debug.log*

# Runtime / PM2
pids
*.pid
.pm2

# Uploaded customer storage (do not commit user data) — anchored to
# backend/storage/ only, so it doesn't also swallow backend/services/storage/
# (source code for the File Storage product).
/storage/

# OS / editor
.DS_Store
.vscode/*
.idea
