{
  "name": "murzak-terminal-broker",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal, non-internet-facing broker that exec's into a tenant's own container via a docker-socket-proxy. Isolated from the payment backend by design.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node test/resolve.test.js && node test/exec.test.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "ws": "^8.18.0"
  }
}
